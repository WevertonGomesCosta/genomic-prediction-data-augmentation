---
title: "6. Data Augmentation — completar T100"
author: "Weverton Gomes da Costa"
date: "`r Sys.Date()`"
site: workflowr::wflow_site
output:
  workflowr::wflow_html:
    toc: true
    toc_depth: 3
    toc_float: true
    code_download: true
    code_folding: show
editor_options:
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = TRUE,
  warning = FALSE,
  message = FALSE,
  cache = FALSE,
  fig.align = "center",
  out.width = "100%"
)
dir.create("output/checkpoints", showWarnings = FALSE, recursive = TRUE)
dir.create("results", showWarnings = FALSE, recursive = TRUE)
```

## 1. Objetivo

Na Etapa 5 vimos que T75, com 827 indivíduos reais, já é equivalente a T100.
Agora testamos se Data Augmentation permite usar ainda menos indivíduos reais.

A regra é simples: começamos com uma amostra real reduzida e criamos exatamente
o número de pseudoindivíduos necessário para completar o tamanho de T100.

\[
n_{syn}=1103-n_{real}.
\]

Assim:

| Cenário | Reais | Sintéticos | Total |
|---|---:|---:|---:|
| T25 + DA | 275 | 828 | 1.103 |
| T50 + DA | 551 | 552 | 1.103 |
| T75 + DA | 827 | 276 | 1.103 |

Os 276 indivíduos de validação continuam fora do treinamento.

## 2. O que é Mixup?

Um pseudoindivíduo é criado combinando dois indivíduos reais distintos do
conjunto de treinamento.

Para os marcadores:

\[
\widetilde{M}=\lambda M_i+(1-\lambda)M_j.
\]

Para o fenótipo:

\[
\widetilde{y}=\lambda y_i+(1-\lambda)y_j.
\]

O pseudoindivíduo não é um novo indivíduo biológico observado. Ele é uma
interpolação matemática entre dois indivíduos reais.

Nenhum ranking fenotípico é usado para escolher os indivíduos reais ou os
dois doadores. Todos são amostrados aleatoriamente dentro do conjunto real de
treinamento.

## 3. Como escolhemos lambda?

Testamos quatro regras:

- `lambda = 0.5`: todos os pseudoindivíduos são misturas 50/50;
- `lambda ~ Beta(0.1, 0.1)`;
- `lambda ~ Beta(0.2, 0.2)`;
- `lambda ~ Beta(0.4, 0.4)`.

Nas distribuições Beta simétricas, a média de `lambda` continua próxima de 0,5,
mas cada pseudoindivíduo pode receber uma intensidade de mistura diferente.
Valores pequenos de `alpha` concentram mais probabilidade perto de 0 e 1.

## 4. Resultados pesados e reprodução

Os 360 modelos já foram executados e auditados. Para manter o site leve, a
execução pesada fica desligada por padrão e a página utiliza o resumo final
aprovado em `results/06_da_summary.csv`.

O código completo continua visível. Para reproduzir todos os modelos do zero,
altere `RUN_MODELS` para `TRUE` depois de executar as Etapas 1, 2 e 3.

```{r configuracao-execucao}
RUN_MODELS <- FALSE

CHECKPOINT <- "output/checkpoints/06_da_progress.csv"
RAW_DA_FILE <- "results/06_da_results.csv"
SUMMARY_FILE <- "results/06_da_summary.csv"
```

## 5. Configurações avaliadas

```{r configuracoes-da}
configs <- data.frame(
  config_mistura = c(
    "lambda_fixo_0.5",
    "beta_alpha_0.1",
    "beta_alpha_0.2",
    "beta_alpha_0.4"
  ),
  tipo_mistura = c(
    "fixed",
    "beta",
    "beta",
    "beta"
  ),
  alpha = c(
    NA,
    0.10,
    0.20,
    0.40
  ),
  stringsAsFactors = FALSE
)

knitr::kable(
  configs,
  caption = "Regras de mistura avaliadas"
)
```

## 6. Código completo do Data Augmentation

O bloco abaixo mostra explicitamente o procedimento usado em cada uma das 360
combinações. Não há função auxiliar escondendo a geração dos pseudoindivíduos
ou a expansão da matriz genômica.

```{r executar-data-augmentation}
if (RUN_MODELS) {
  arquivos_necessarios <- c(
    "output/01_data_objects.rds",
    "output/02_splits_mestre_30rep.rds",
    "output/03_genomic_objects.rds"
  )

  if (!all(file.exists(arquivos_necessarios))) {
    stop("Execute primeiro as Etapas 1, 2 e 3.")
  }

  if (!requireNamespace("BGLR", quietly = TRUE)) {
    stop("Instale o pacote BGLR antes de reproduzir os modelos.")
  }

  obj <- readRDS("output/01_data_objects.rds")
  splits_mestre <- readRDS("output/02_splits_mestre_30rep.rds")
  gobj <- readRDS("output/03_genomic_objects.rds")

  y <- obj$y
  M <- obj$M
  n <- obj$n

  p <- gobj$p
  Z <- gobj$Z
  G <- gobj$G
  denominador <- gobj$denom

  N_T100 <- 1103
  PROPORCOES <- c(0.25, 0.50, 0.75)
  N_ITER <- 10000
  BURN_IN <- 5000

  if (file.exists(CHECKPOINT)) {
    resultados_da <- read.csv(CHECKPOINT)
  } else {
    resultados_da <- data.frame()
  }

  for (ci in seq_len(nrow(configs))) {
    for (proporcao in PROPORCOES) {
      for (rep in seq_len(30)) {

        # Uma chave identifica unicamente cada modelo.
        ja_executado <-
          nrow(resultados_da) > 0 &&
          any(
            resultados_da$config_mistura == configs$config_mistura[ci] &
            resultados_da$proporcao_pool == proporcao &
            resultados_da$repeticao == rep
          )

        if (ja_executado) next

        split <- splits_mestre[[as.character(proporcao)]][[rep]]
        treino <- split$treino
        validacao <- split$valid

        M_train <- M[treino, , drop = FALSE]
        y_train <- y[treino]

        n_real <- length(treino)
        n_syn <- N_T100 - n_real

        stopifnot(n_real + n_syn == N_T100)

        # ------------------------------------------------------------
        # 1. Sorteio de dois doadores reais distintos
        # ------------------------------------------------------------
        seed_da <-
          200000 +
          ci * 10000 +
          as.integer(proporcao * 100) * 100 +
          rep

        set.seed(seed_da)

        idx1 <- sample(
          seq_len(n_real),
          n_syn,
          replace = TRUE
        )

        idx2_base <- sample(
          seq_len(n_real - 1),
          n_syn,
          replace = TRUE
        )

        idx2 <- ifelse(
          idx2_base >= idx1,
          idx2_base + 1L,
          idx2_base
        )

        stopifnot(all(idx1 != idx2))

        # ------------------------------------------------------------
        # 2. Valor de lambda de cada pseudoindivíduo
        # ------------------------------------------------------------
        if (configs$tipo_mistura[ci] == "fixed") {
          lambda <- rep(
            0.5,
            n_syn
          )
        } else {
          lambda <- rbeta(
            n_syn,
            shape1 = configs$alpha[ci],
            shape2 = configs$alpha[ci]
          )
        }

        # ------------------------------------------------------------
        # 3. Genótipos e fenótipos sintéticos
        # ------------------------------------------------------------
        M_syn <-
          lambda * M_train[idx1, , drop = FALSE] +
          (1 - lambda) * M_train[idx2, , drop = FALSE]

        y_syn <-
          lambda * y_train[idx1] +
          (1 - lambda) * y_train[idx2]

        # ------------------------------------------------------------
        # 4. Expansão da matriz G
        # ------------------------------------------------------------
        n_total_matriz <- n + n_syn
        idx_syn <- (n + 1):(n + n_syn)

        G_expanded <- matrix(
          0,
          nrow = n_total_matriz,
          ncol = n_total_matriz
        )

        # O bloco dos indivíduos reais permanece exatamente igual.
        G_expanded[
          seq_len(n),
          seq_len(n)
        ] <- G

        # Os sintéticos usam as mesmas frequências alélicas e a mesma escala.
        Z_syn <- sweep(
          M_syn,
          2,
          2 * p,
          FUN = "-"
        )

        G_syn_real <- tcrossprod(
          Z_syn,
          Z
        ) / denominador

        G_syn_syn <- tcrossprod(
          Z_syn
        ) / denominador

        G_expanded[
          idx_syn,
          seq_len(n)
        ] <- G_syn_real

        G_expanded[
          seq_len(n),
          idx_syn
        ] <- t(G_syn_real)

        G_expanded[
          idx_syn,
          idx_syn
        ] <- G_syn_syn

        G_expanded[
          cbind(idx_syn, idx_syn)
        ] <-
          G_expanded[cbind(idx_syn, idx_syn)] +
          1e-6

        stopifnot(
          max(
            abs(G_expanded - t(G_expanded))
          ) < 1e-8
        )

        stopifnot(
          max(
            abs(
              G_expanded[seq_len(n), seq_len(n)] - G
            )
          ) < 1e-12
        )

        # ------------------------------------------------------------
        # 5. Fenótipos observados pelo modelo
        # ------------------------------------------------------------
        y_modelo <- rep(
          NA_real_,
          n_total_matriz
        )

        y_modelo[treino] <- y_train
        y_modelo[idx_syn] <- y_syn

        reais_fora_treino <- setdiff(
          seq_len(n),
          treino
        )

        stopifnot(all(is.na(y_modelo[validacao])))
        stopifnot(all(is.na(y_modelo[reais_fora_treino])))
        stopifnot(sum(!is.na(y_modelo)) == N_T100)

        # ------------------------------------------------------------
        # 6. GBLUP com a matriz expandida
        # ------------------------------------------------------------
        ETA <- list(
          list(
            K = G_expanded,
            model = "RKHS"
          )
        )

        seed_bglr <-
          260000 +
          ci * 10000 +
          as.integer(proporcao * 100) * 100 +
          rep

        set.seed(seed_bglr)

        ajuste <- BGLR::BGLR(
          y = y_modelo,
          ETA = ETA,
          nIter = N_ITER,
          burnIn = BURN_IN,
          verbose = FALSE
        )

        # ------------------------------------------------------------
        # 7. Predição somente nos indivíduos reais da validação
        # ------------------------------------------------------------
        predicao <- ajuste$yHat[validacao]
        observado <- y[validacao]

        correlacao <- cor(
          observado,
          predicao
        )

        RMSE <- sqrt(
          mean(
            (observado - predicao)^2
          )
        )

        MAE <- mean(
          abs(
            observado - predicao
          )
        )

        calibracao <- lm(
          observado ~ predicao
        )

        slope_calibracao <- unname(
          coef(calibracao)[2]
        )

        nova_linha <- data.frame(
          config_mistura = configs$config_mistura[ci],
          tipo_mistura = configs$tipo_mistura[ci],
          alpha = configs$alpha[ci],
          repeticao = rep,
          proporcao_pool = proporcao,
          n_real = n_real,
          n_syn = n_syn,
          n_total_treino = n_real + n_syn,
          correlacao = correlacao,
          RMSE = RMSE,
          MAE = MAE,
          slope_calibracao = slope_calibracao,
          media_lambda = mean(lambda),
          sd_lambda = sd(lambda)
        )

        resultados_da <- rbind(
          resultados_da,
          nova_linha
        )

        # Um resultado concluído é salvo imediatamente.
        write.csv(
          resultados_da,
          CHECKPOINT,
          row.names = FALSE
        )
      }
    }
  }

  # Auditoria mínima antes de declarar a execução completa.
  chaves_da <- paste(
    resultados_da$config_mistura,
    resultados_da$proporcao_pool,
    resultados_da$repeticao,
    sep = "__"
  )

  stopifnot(nrow(resultados_da) == 360)
  stopifnot(anyDuplicated(chaves_da) == 0)
  stopifnot(all(resultados_da$n_total_treino == 1103))
  stopifnot(
    !anyNA(
      resultados_da[
        ,
        c(
          "correlacao",
          "RMSE",
          "MAE",
          "slope_calibracao"
        )
      ]
    )
  )

  write.csv(
    resultados_da,
    RAW_DA_FILE,
    row.names = FALSE
  )
}
```

## 7. Resumo final aprovado

O arquivo abaixo contém o resumo dos 360 ajustes auditados. As diferenças
`DA - real` usam o baseline da mesma proporção. As diferenças contra T100 usam
o benchmark real de 1.103 indivíduos.

```{r carregar-resumo-final}
if (!file.exists(SUMMARY_FILE)) {
  stop("Arquivo ausente: results/06_da_summary.csv")
}

resumo_da <- read.csv(
  SUMMARY_FILE
)

stopifnot(nrow(resumo_da) == 12)

resumo_exibicao <- resumo_da[
  order(
    resumo_da$proporcao_pool,
    -resumo_da$mean_cor
  ),
  c(
    "config_mistura",
    "n_real",
    "n_syn",
    "mean_cor",
    "sd_cor",
    "mean_RMSE",
    "mean_MAE",
    "mean_slope",
    "delta_cor_vs_same_real"
  )
]

knitr::kable(
  resumo_exibicao,
  digits = 4,
  caption = "Resumo dos 360 ajustes de Data Augmentation"
)
```

Para T25 e T50, a configuração `Beta(0.2, 0.2)` apresentou a maior correlação
média entre as regras de DA. Para T75, `lambda = 0.5` apresentou o melhor
resultado. Entretanto, em todos os casos a correlação média permaneceu abaixo
do baseline real correspondente.

## 8. DA versus o mesmo tamanho real

Esta é a comparação que identifica se o Mixup acrescentou informação útil além
da amostra real já disponível.

```{r da-vs-real}
comparacao_real <- resumo_da[
  ,
  c(
    "config_mistura",
    "proporcao_pool",
    "n_real",
    "mean_cor",
    "delta_cor_vs_same_real"
  )
]

knitr::kable(
  comparacao_real[
    order(
      comparacao_real$proporcao_pool,
      -comparacao_real$mean_cor
    ),
  ],
  digits = 4,
  caption = "Mudança de correlação causada pelo DA em relação ao mesmo tamanho real"
)
```

Todos os valores médios de `delta_cor_vs_same_real` são negativos. Portanto,
nenhuma das quatro regras de Mixup melhorou, em média, o GBLUP ajustado com o
mesmo conjunto de indivíduos reais.

## 9. Equivalência com T100

Aplicamos a mesma lógica de equivalência da Etapa 5. O interesse agora é saber
se T25 + DA ou T50 + DA conseguem entrar completamente na margem de ±0,05 em
relação a T100.

Os intervalos abaixo são os intervalos de 90% com correção de Nadeau-Bengio da
execução final auditada.

```{r equivalencia-da}
equivalencia_da <- resumo_da[
  ,
  c(
    "config_mistura",
    "n_real",
    "n_syn",
    "delta_cor_vs_T100_mean",
    "ic90_inf",
    "ic90_sup",
    "equivalente_T100"
  )
]

knitr::kable(
  equivalencia_da[
    order(
      equivalencia_da$n_real,
      -equivalencia_da$delta_cor_vs_T100_mean
    ),
  ],
  digits = 4,
  caption = "Equivalência das estratégias de DA com T100"
)
```

Nenhuma estratégia com 275 ou 551 indivíduos reais foi equivalente a T100.
Algumas estratégias T75 + DA permaneceram dentro da margem, mas T75 já era
equivalente sem Data Augmentation.

## 10. Conclusão final

Sem DA, o menor conjunto equivalente a T100 foi:

\[
827\text{ indivíduos reais}.
\]

Com as quatro regras de Mixup avaliadas, o menor conjunto real que permaneceu
na região de equivalência também foi T75. Portanto:

\[
\boxed{\text{redução adicional atribuível ao DA}=0}.
\]

Sob este desenho, completar amostras aleatórias reduzidas até T100 com
pseudoindivíduos Mixup não substituiu a informação dos indivíduos reais
retirados. Esse resultado não significa que todo método de Data Augmentation
seja ineficaz; ele descreve especificamente as estratégias de Mixup avaliadas
neste tutorial.
